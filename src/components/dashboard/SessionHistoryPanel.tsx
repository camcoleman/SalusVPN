import SessionHistory from "@/components/SessionHistory";

export default function SessionHistoryPanel() {
  return (
    <section className="rounded-xl border border-border bg-surface p-1">
      <SessionHistory />
    </section>
  );
}
